apiVersion: v1
kind: Pod
metadata:
  name: simple-webapp
  labels: # 라벨은 원하는 만큼 가능sss
    app: App1
    function: Front-end

spec:
  containers:
  - name: simple-webapp
    image: simple-webapp
    ports:
      - containerPort: 8080